# 部署与资源上传指南

这份文档说明两件事：网页怎么发布，视频怎么放腾讯云 COS。

---

## 一、文件分工（重要）

网站资源分两类，放在不同地方：

**A. 小文件 —— 跟网页一起发布到 GitHub Pages**

```
index.html  works.html  resume.html
styles.css  app.js  content.js
assets/fonts/noto-serif-sc-subset.woff2   ← 标题字体，93KB
```

这些必须和 HTML 在一起，别放 COS。字体尤其不能动，它是靠 CSS 里的相对路径 `assets/fonts/` 加载的。

**B. 大文件 —— 放腾讯云 COS**

```
assets/videos/*.mp4        ← 视频，最大的两支各 6MB+
assets/images/*.jpg        ← 封面图
```

> 说明：如果视频都不大（总共十几 MB），其实也可以全放 GitHub Pages，不用 COS。
> COS 的意义是：单文件超过几 MB、或访客多时，CDN 加载更快更稳。

---

## 二、把视频放到腾讯云 COS

### 步骤 1：上传，保持目录结构

在 COS 控制台里，把整个 `assets` 文件夹传上去，**保持原来的目录结构**：

```
你的桶/
  assets/
    videos/
      hero.mp4
      work-01.mp4
      ...
    images/
      hero-poster.jpg
```

关键：COS 上的路径要和本地一致（都在 `assets/videos/` 下）。代码会自动拼接，你不用改每一条链接。

### 步骤 2：在 content.js 填入桶域名

打开 `content.js`，最上面第一个配置项：

```js
window.SITE = {
  assetBase: '',   // ← 改这里
```

改成你的 COS 桶域名（在 COS 控制台「概览」页能看到，形如）：

```js
  assetBase: 'https://你的桶名-1250000000.cos.ap-guangzhou.myqcloud.com',
```

注意：
- 末尾**不要**加斜杠
- 必须是 `https://`（GitHub Pages 是 HTTPS，加载 HTTP 视频会被浏览器拦掉）
- 改完这一处就够了，各作品的 `video` 字段保持 `assets/videos/xxx.mp4` 不动

### 步骤 3：COS 控制台三项配置（否则视频加载不出来）

1. **公有读**
   桶设置 → 权限管理 → 把「公共权限」设为 **公有读私有写**。
   否则别人打开你的作品集时视频是 403。

2. **CORS 跨域**
   桶设置 → 跨域访问 CORS → 添加规则：
   -来源 Origin：`https://你的用户名.github.io`（你的 Pages 域名；图省事可先填 `*` 测试）
   - Methods：`GET`、`HEAD`
   - Allow-Headers：`*`
   - 视频跨域播放、以及 JS 读取时长需要这个。

3. **防盗链（可选）**
   如果开了防盗链，把你的 Pages 域名加进白名单，否则同样加载失败。
   不确定就先别开。

### 步骤 4：验证

改完 `content.js` 后本地打开 works.html，点开任意作品：
- 视频能播 → 成功
- 视频转圈/报错 → 打开浏览器控制台（F12）看报错：
  - `403` → 第 3 步公有读没设
  - `CORS` 字样 → 第 3 步 CORS 没配对
  - `mixed content` → 桶域名写成了 http，改成 https

---

## 三、视频已做的优化（faststart）

`assets/videos/` 里的 6 支视频已经用 `ffmpeg -movflags +faststart` 处理过，
把索引（moov）挪到了文件开头。作用：浏览器不用下完整个文件就能开始播第一帧。

**以后你换真实视频时，上传 COS 前先跑一遍这个处理**，否则大视频在 COS 上会「点开要等很久」：

```bash
# 单个文件
ffmpeg -i 原视频.mp4 -c copy -movflags +faststart 处理后.mp4

# 批量处理某文件夹
cd assets/videos
for f in *.mp4; do
  ffmpeg -y -i "$f" -c copy -movflags +faststart "/tmp/$f" && mv "/tmp/$f" "$f"
done
```

`-c copy` 表示不重新编码、无损、几秒完成，只是挪一下索引位置。

---

## 四、改了标题文案后，刷新字体

标题用的是子集化字体，只包含**当前文案里出现过的字**。
如果你改了首页/作品页/简历页的标题，或在 content.js 里写了新的中文标题，
用到新字时那个字会掉回系统字体（看起来会有一个字突兀）。

这时重新生成字体子集：

```bash
python3 tools/build-fonts.py
```

它会重新扫描所有页面用字、重新打包。正文和描述不受影响，只有标题（衬线大字）走这个字体。

---

## 五、发布到 GitHub Pages

你的网络推不动 GitHub（SSH/HTTPS 都超时），所以走网页手动上传：

1. 打开你的仓库 → 上传这些文件，保持目录结构：
   `index.html`、`works.html`、`resume.html`、`styles.css`、`app.js`、`content.js`、
   `assets/fonts/noto-serif-sc-subset.woff2`
   （视频若走 COS 就不用传 `assets/videos`；封面同理）
2. 仓库 Settings → Pages → Source 选 `main` 分支 → Save
3. 等一两分钟，访问 `https://你的用户名.github.io/仓库名/`

> 定稿前记得删掉三个 HTML 里的主色切换器：
> 每个文件搜 `data-mount="accent"`，删掉那一行 `<div>`。
